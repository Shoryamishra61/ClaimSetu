# Routes

The app is a Vite React single-page application with no router dependency.

| URL | Entry | Layout |
|---|---|---|
| `/` | `apps/web/src/main.tsx` (to be created) | Focused custody handover flow |

API routes are same-origin under `/api/v1`; Vite proxies them to the FastAPI app.
