# Shared layouts

No rendered layout exists yet. `apps/web/index.html` mounts `src/main.tsx`, which
is not implemented. The new target requires a focused service shell: skip link,
prototype disclosure, compact header/language control, single-column task area,
and quiet footer. It must not use a dashboard/sidebar paradigm.
