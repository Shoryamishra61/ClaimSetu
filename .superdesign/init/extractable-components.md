# Extractable components

No rendered shared components exist yet, so there are no source components to
extract into DraftComponents. Simple primitives should be implemented inline after
the initial new-target draft. The resulting app should establish reusable
ProgressSteps, PrototypeNotice, Field, StatusBadge, and SourceDrawer components.
