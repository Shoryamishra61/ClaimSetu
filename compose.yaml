name: identity-rescue

services:
  identity-rescue:
    build: .
    ports:
      - "8000:8000"
    environment:
      IR_BUILD_LABEL: compose-local
      IR_SERVE_FRONTEND: "true"
    restart: unless-stopped
