# Codex build log — Identity Rescue pivot

## 2026-08-22

- Tagged the pre-pivot repository as `handover29c-baseline-2026-08-22` and created `identity-rescue-pivot`.
- Copied the complete frozen Identity Rescue package into `docs/identity-rescue/` and recorded `MIGRATION_MAP.md`.
- Replaced the default citizen product and OpenAPI surface; the former vehicle domain remains quarantined behind an explicit historical flag.
- Implemented exact synthetic profiles, evidence-preserving records, conservative normalization, multi-state findings, versioned deterministic rules, explicit-cost plan search, reversible simulation, and source-backed handoff.
- Implemented three complete React journeys, English/Hindi localization, privacy/source pages, persistent disclosure, evidence drawers, accessible simulation dialog, readiness live region, reset/undo, deep-link routing, mobile reflow, and reduced-motion styling.
- Added focused rule/planner/source/security tests and Playwright golden journeys, including the Scenario B anti-error proof.
- Reworked the Docker/Compose runtime as Identity Rescue, removed default persistence requirements, kept it non-root, and verified same-origin deep-link refresh.
- Added isolated mock authority adapters, a versioned nine-rule registry, conservative Indian-name/date edge handling, rich correction metadata, and a replayable simulation event journal.
- Added every-route accessibility checks, route announcements, deterministic history/reset tests, keyboard simulation, reduced-motion coverage, delayed-network recovery, and slow-4G paint budgets.
- Generated a reproducible 118-second narrated submission MP4 with an explicit citizen-first first minute and architecture/product rationale in the second.

## Verified commands

- `uv run ruff check app tests_identity_rescue`
- `uv run pytest` — 40 passed
- `npm test -- --run` — 5 passed after static-deployment coverage
- `npm run build`
- `npm run e2e` — 10 passed; slow-4G FCP/LCP about 1.45 seconds
- `npm audit --audit-level=high` — 0 vulnerabilities
- Docker build/runtime smoke — five-path OpenAPI, root/deep-route 200, UID 100
- `uvx pip-audit -r requirements.txt` — no known vulnerabilities
- final narrated demo — 118.000 seconds, 1280×720, one audio and one video track, ten FFmpeg review frames
- generated static export — stable SHA-256 across repeated generation; all three live Pages journeys pass with zero external requests

## Honest boundary

The optional OpenAI explanation layer is paused pending a credential decision. It is not needed for any deterministic journey or to satisfy the hackathon brief. Native screen-reader smoke testing and fluent-human Hindi review remain unclaimed. The EPFO PDF timed out from this environment during the latest source reachability pass, so its exact fixture predicate remains explicitly marked `PROTOTYPE_SIMULATION`.

The verified source snapshot and supplemental 23.88-second working capture were published to the public `identity-rescue-pivot` branch. The permanent credential-free demo is live at <https://shoryamishra61.github.io/handover29c/> and was exercised from the deployed origin. The narrated MP4 is tracked with the release evidence. Corrected workflow files remain local because the GitHub OAuth token lacks the separately required `workflow` scope; hosted CI is therefore not claimed.
